<h1>Cocktails#index</h1>
<p>Find me in app/views/cocktails/index.html.erb</p>

<div class="container">
  <div class="row">
    <% @cocktails.each do |cocktail|  %>
      <div class="col-xs-12 col-sm-4">
        <div class="card">

            <h2><%= cocktail.name %></h2>

          <%= link_to "Read more", cocktail_path(cocktail) %>
        </div>
      </div>
    <% end %>
  </div>
</div>
